---
import Base from '../layouts/Base.astro';
---

<Base title="使用教程">
  <section class="mx-auto max-w-3xl px-4 py-16">
    <h1 class="mb-8 text-3xl font-bold">使用教程</h1>

    <div class="space-y-10">
      {[
        {
          step: '1', title: '下载与安装',
          content: `从下载页面获取对应系统的安装包。Windows 用户运行 .msi 安装包，macOS 用户挂载 .dmg 后将应用拖入 Applications 文件夹，Linux 用户运行 .AppImage 或通过包管理器安装 .deb 包。`
        },
        {
          step: '2', title: '设置 Java 运行时',
          content: `启动器首次运行时会自动检测系统中的 Java。你也可以在设置页面手动指定 Java 路径，或使用内置的 Java 下载功能自动安装兼容版本。`
        },
        {
          step: '3', title: '添加账户',
          content: `点击侧边栏的「账户」进入账户管理页面。支持的登录方式包括：Microsoft 账户（正版登录）、离线账户（自定义用户名）、外置登录（兼容 Authlib-Injector 的认证服务器）。`
        },
        {
          step: '4', title: '安装游戏实例',
          content: `在实例页面点击「创建实例」，选择想要安装的 Minecraft 版本和模组加载器（Forge/Fabric/Quilt/NeoForge 等）。启动器会自动下载所需文件。`
        },
        {
          step: '5', title: '管理模组与资源包',
          content: `在实例详情页可以管理模组、资源包、光影包、数据包等。支持从 Modrinth、CurseForge 等主流平台直接搜索和安装。`
        },
        {
          step: '6', title: '启动游戏',
          content: `确认 Java 路径和内存分配无误后，点击「启动」即可进入游戏。启动器会实时显示启动进度，并在游戏窗口出现后自动转入后台运行监控。`
        },
      ].map(item => (
        <div class="flex gap-4">
          <div class="flex size-8 shrink-0 items-center justify-center rounded-full bg-[var(--color-primary)] text-sm font-bold text-[var(--color-bg)]">
            {item.step}
          </div>
          <div>
            <h2 class="mb-2 text-xl font-semibold">{item.title}</h2>
            <p class="text-[var(--color-muted-fg)] leading-relaxed">{item.content}</p>
          </div>
        </div>
      ))}
    </div>

    <div class="mt-10 rounded-xl border border-[var(--color-border)] bg-[var(--color-card)] p-5">
      <h3 class="mb-2 font-semibold">进阶提示</h3>
      <ul class="space-y-1 text-sm text-[var(--color-muted-fg)]">
        <li>• 版本隔离功能：开启后，每个实例的 mods/saves 等目录完全独立</li>
        <li>• 资源中心：一站式查找和安装各类 Minecraft 资源</li>
        <li>• 联机功能：通过 EasyTier 实现内网穿透联机</li>
      </ul>
    </div>
  </section>
</Base>
